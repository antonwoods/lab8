<% if current_user.admin? %>

<%= form_for @idea do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
your status: {officer}

<%else%>
your status: {member}
<% end %>